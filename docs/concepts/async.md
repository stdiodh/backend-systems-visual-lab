# Async Concepts

### 비동기 연동

분류: 시스템 구조

정의: 요청 처리 흐름에서 모든 작업을 즉시 완료하지 않고 일부 작업을 나중에 별도 흐름에서 처리하는 방식이다.

실무에서 중요한 이유: 응답 시간을 줄이고 실패 지점을 분리할 수 있다.

흔한 오해: 비동기는 무조건 빠르거나 무조건 좋은 방식이라고 생각한다.

Visual Lab 반영 방식: Sync vs Async Split View로 요청 흐름과 후속 처리 흐름을 분리한다.

Codex 누락 방지 규칙: 유실, 중복 처리, 순서 문제를 함께 넣는다.

### 메시징

분류: 비동기 / 시스템 연동

정의: 생산자가 메시지를 발행하고 소비자가 나중에 처리하도록 중간 브로커나 큐를 두는 방식이다.

실무에서 중요한 이유: 시스템 사이의 직접 의존과 장애 전파를 줄일 수 있다.

흔한 오해: 메시지를 쓰면 데이터 정합성 문제가 사라진다고 생각한다.

Visual Lab 반영 방식: Producer, Broker, Consumer, Retry/DLQ 흐름을 보여준다.

Codex 누락 방지 규칙: 최소 한 번 처리, 중복 처리, 순서 보장 여부를 명시한다.

### 트랜잭션 아웃박스 패턴

분류: 비동기 / 데이터 정합성

정의: 비즈니스 데이터 변경과 이벤트 발행 요청을 같은 DB 트랜잭션에 저장한 뒤 별도 릴레이가 메시지로 발행하는 패턴이다.

실무에서 중요한 이유: DB 저장은 성공했지만 메시지 발행은 실패하는 불일치를 줄인다.

흔한 오해: DB 저장 후 바로 메시지를 보내면 충분하다고 생각한다.

Visual Lab 반영 방식: DB 저장, outbox 저장, relay, message broker, consumer 흐름을 단계별로 보여준다.

Codex 누락 방지 규칙: 실패 시 재처리와 중복 처리 가능성을 반드시 포함한다.

### CDC

분류: 비동기 / 데이터 변경 전파

정의: 데이터베이스 변경 로그를 감지해 다른 시스템으로 전달하는 방식이다.

실무에서 중요한 이유: 애플리케이션 코드가 직접 이벤트를 발행하지 않아도 데이터 변경을 기반으로 연동할 수 있다.

흔한 오해: CDC가 모든 메시징 문제를 대체한다고 생각한다.

Visual Lab 반영 방식: DB Change Log에서 Relay를 거쳐 Consumer로 가는 흐름을 보여준다.

Codex 누락 방지 규칙: CDC는 05장 비동기 연동의 선택지 중 하나로 표시한다.

